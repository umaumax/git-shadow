#compdef git-shadow

_git_shadow() {
  emulate -L zsh

  _values \
    'subcommand' \
    'add[add local clone(shadow) repo: REPO_DIR]' \
    'sync[sync current branch]' \
    'list[list shadow repo dirs]' \
    'remove[WIP]'

  # NOTE: at _git_xxx _files are added automatically
  # _files
}
